# PR 19 review corrections

Date: 2026-09-07. Scope: four automated review findings on commit `0035c7e`.

- IMP-001 (P1): The evaluator accepted smoke evidence from an older commit. Paid decision mode now requires the current commit to match the frozen evidence source. A dry run remains available, but stale evidence cannot authorize promotion. The regression test confirms rejection before any worker starts. This conservative check also rejects documentation-only revisions. Production effort remains `high`. No new paid campaign ran.
- IMP-002 (P1): Cache reads converted stored pounds to kilograms. The cache now returns a pound-based type. Ingestion converts Hevy records once. Tests confirm that repeated storage reads preserve fractional pound values and produce the same workout summary.
- IMP-003 (P1 in the review, documentation severity): The logging contract omitted the new telemetry prefixes. AGENTS.md now defines their purposes and forbids payload bodies and credentials. Existing production log filters remain valid.
- IMP-004 (P2): A missing history array produced a false empty-history result. The client now returns an API error for malformed response envelopes. Tests cover missing, null, and non-array fields, plus valid empty arrays in both supported naming formats.

Verification: all 93 offline tests passed. Both TypeScript checks and the whitespace check passed. No production process restart or paid model test occurred.

The initial production check predates these corrections. The automated tests exercise synchronization and history reads with local fixtures. Live confirmation remains a normal-use check after deployment.
